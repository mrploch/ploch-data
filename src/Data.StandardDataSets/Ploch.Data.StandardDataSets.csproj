<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <!-- Overrides the solution-wide default in Directory.Build.props.
         Reference data sets - no Entity Framework Core or repository surface. -->
    <PackageTags>ploch;data;seed-data;reference-data;lookup;countries;regions;dotnet</PackageTags>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <None Include=".\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
