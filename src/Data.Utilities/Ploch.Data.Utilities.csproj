<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
      <PackageReadmeFile>README.md</PackageReadmeFile>
      <!-- Overrides the solution-wide default in Directory.Build.props.
           No Entity Framework Core surface - this package only extends System.Data types. -->
      <PackageTags>ploch;data;system-data;datatable;datacolumn;utilities;dotnet</PackageTags>
      <TargetFramework>netstandard2.0</TargetFramework>
    </PropertyGroup>
    <ItemGroup>
      <None Include=".\README.md" Pack="true" PackagePath="\" />
    </ItemGroup>
</Project>
